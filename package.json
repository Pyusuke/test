{
  "name": "parts-search",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-site parts search application",
  "scripts": {
    "dev": "pnpm --filter @parts-search/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
